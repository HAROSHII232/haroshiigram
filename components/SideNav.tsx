import Logo from "./Logo";

function SideNav() {
  return (
    <div>
      <div>
        <Logo />
        {/* <NavLinks/> */}
        {/* user && <ProfileLink> */}

        <div>{/* <MoreDropdown/> */}</div>
      </div>
    </div>
  );
}

export default SideNav;
